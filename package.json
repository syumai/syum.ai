{
  "name": "@syumai/syum.ai",
  "private": true,
  "scripts": {
    "dev": "spin up --listen 127.0.0.1:18787",
    "build": "spin build",
    "deploy": "spin aka deploy",
    "watch": "go tool templ generate --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.15",
    "tailwindcss": "^4.0.15"
  }
}
