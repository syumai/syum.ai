{
  "name": "@syumai/syum.ai",
  "private": true,
  "scripts": {
    "dev": "air .",
    "build": "go run github.com/syumai/workers/cmd/workers-assets-gen && tinygo build -o ./build/app.wasm -target wasm -panic=trap -no-debug -gc=leaking -opt=2 ./...",
    "deploy": "wrangler deploy",
    "watch": "go tool templ generate --watch",
    "generate": "tailwindcss -o ./public/css/style.css && go tool templ generate"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.15",
    "tailwindcss": "^4.0.15",
    "wrangler": "^4.3.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild",
      "workerd"
    ],
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
